version: '3.8'

services:
  volguard:
    build: .
    container_name: volguard
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - volguard-data:/app/data
      - volguard-logs:/app/logs
    restart: unless-stopped

volumes:
  volguard-data:
  volguard-logs:
